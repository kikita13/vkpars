import { FIELDS } from "@consts/fields";
import { useCodePosts } from "@consts/hooks/slicedCode";

const not = () => {


  return (
    <div className='container'>
      This page is not found (code 404)
    </div>
  );
};

export default not;